'use strict';

/**
 * properties-listings-page router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::properties-listings-page.properties-listings-page');
