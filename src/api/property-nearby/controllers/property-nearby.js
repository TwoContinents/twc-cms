'use strict';

/**
 * property-nearby controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::property-nearby.property-nearby');
