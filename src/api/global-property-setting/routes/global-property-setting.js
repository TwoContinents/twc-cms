'use strict';

/**
 * global-property-setting router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::global-property-setting.global-property-setting');
