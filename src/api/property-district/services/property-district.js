'use strict';

/**
 * property-district service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::property-district.property-district');
