'use strict';

/**
 * properties-listings-page controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::properties-listings-page.properties-listings-page');
